name: Manual Trigger Test

on:
  workflow_dispatch: # 수동 실행용 트리거

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "🎉 GitHub Actions 수동 실행 성공!"
